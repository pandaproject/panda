{% if root_disk_free < settings.PANDA_AVAILABLE_SPACE_WARN %}{% blocktrans with root_disk_free|filesizeformat as bytes and root_disk_percent_used|floatformat as percent %}The drive containing your PANDA application is CRITICALLY low on disk space: {{ bytes }} remains ({{ percent }}% full){% endblocktrans %}{% elif root_disk_free < settings.PANDA_AVAILABLE_SPACE_WARN %}{% blocktrans with root_disk_free|filesizeformat as bytes and root_disk_percent_used|floatformat as percent %}The drive containing your PANDA application is low on disk space: {{ bytes }} remains ({{ percent }}% full){% endblocktrans %}{% endif %}

{% if if upload_disk_total != None and upload_disk_free < settings.PANDA_AVAILABLE_SPACE_WARN %}{% blocktrans with upload_disk_free|filesizeformat as bytes and upload_disk_percent_used|floatformat as percent %}The drive containing your PANDA uploads is CRITICALLY low on disk space: {{ bytes }} remains ({{ percent }}% full){% endblocktrans %}{% elif if upload_disk_total != None and upload_disk_free < settings.PANDA_AVAILABLE_SPACE_WARN %}{% blocktrans with upload_disk_free|filesizeformat as bytes and upload_disk_percent_used|floatformat as percent %}The drive containing your PANDA uploads is low on disk space: {{ bytes }} remains ({{ percent }}% full){% endblocktrans %}{% endif %}

{% if if indices_disk_total != None and indices_disk_free < settings.PANDA_AVAILABLE_SPACE_WARN %}{% blocktrans with indices_disk_free|filesizeformat as bytes and indices_disk_percent_used|floatformat as percent %}The drive containing your PANDA search indices is CRITICALLY low on disk space: {{ bytes }} remains ({{ percent }}% full){% endblocktrans %}{% elif if indices_disk_total != None and indices_disk_free < settings.PANDA_AVAILABLE_SPACE_WARN %}{% blocktrans with indices_disk_free|filesizeformat as bytes and indices_disk_percent_used|floatformat as percent %}The drive containing your PANDA search indices is low on disk space: {{ bytes }} remains ({{ percent }}% full){% endblocktrans %}{% endif %}

{% trans "Your can always check your current disk usage on your PANDA Dashboard:" %}
http://{{ site_domain }}/#dashboard
