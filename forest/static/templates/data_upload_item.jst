<li>
    <strong><a href="/api/1.0/related_upload/<%= upload.id %>/download/"><%= upload.original_filename %></a></strong>
    (<strong><%= upload.size %> bytes</strong>)
    uploaded by <strong><%= upload.creator.first_name %> <%= upload.creator.last_name %></strong> &lt;<a href="mailto:<%= upload.creator.email %>"><%= upload.creator.email %></a>&gt;
    on <strong><%= moment(upload.creation_date, "YYYY-MM-DDTHH:mm:ss").toLocalTimezone().format("MMMM D, YYYY @ h:mm a") %></strong>
</li>

