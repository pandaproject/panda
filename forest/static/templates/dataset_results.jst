<% if (!query) { %>

    Please enter a query.

<% } else if (dataset.total_count == 0) { %>

    No results found.

<% } else { %>

    <% pager = PANDA.templates.dataset_pager({ dataset: dataset, query: query }); %>
    <% print(pager); %>

    <% print(PANDA.templates.dataset_block(dataset)); %>

    <% print(pager); %>

<% } %>

