<div id="list-datasets">
    <h1>Datasets</h1>

    <%= pager %>

    <% _.each(datasets, function(dataset) { %>
        <% print(dataset_template(dataset)); %>
    <% }); %>

    <%= pager %>
</div>
