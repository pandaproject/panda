<% if (!query) { %>

    Please enter a query.

<% } else if (total_count == 0) { %>

    No results found.

<% } else { %>

<p>Showing <%= offset + 1 %> through <% if(offset + limit < total_count) { print(offset + limit); } else { print(total_count); } %> of <%= total_count %> groups</p>

<% _.each(datasets, function(dataset) { %>
    <h3><a href="#dataset/<%= dataset.id %>"><%= dataset.name %></a> &mdash; <%= dataset.total_count %> results</h3>
    <table>
        <tr>
            <% _.each(dataset.schema, function(s) { %>
            <th><%= s.column %></th>
            <% }); %>
        </tr>

        <% _.each(dataset.data, function(d) { %>
        <tr>
            <% _.each(d.data, function(datum) { %>
            <td><%= datum %></td>
            <% }); %>
        </tr>
        <% }); %>
    </table>
    <% if (dataset.next) { %>
    <strong>TKTK - more data available, group pagination here</strong>
    <% } %>
<% }); %>

<div id="search-pager">
    <% if (previous) { %>
        <a href="#search/<%= query %>/<%= limit %>/<%= page %>" class="prev">&laquo;</a> 
    <% } else { %>
        <span>&laquo;</span> 
    <% } %>

    <%= page + 1 %>

    <% if (next) { %>
        <a href="#search/<%= query %>/<%= limit %>/<%= page + 2 %>" class="prev">&raquo;</a>
    <% } else { %>
        <span>&raquo;</span>
    <% } %>
</div>

<% } %>
